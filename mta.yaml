_schema-version: 3.3.0
ID: books1
description: A simple CAP project.
version: 1.0.0
modules:
- name: books1-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: books1-db
  - name: uaa_books1
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: books1-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: books1-db
  parameters:
    buildpack: nodejs_buildpack
- name: books1-destination-content
  type: com.sap.application.content
  requires:
  - name: books1-destination-service
    parameters:
      content-target: true
  - name: books1_html_repo_host
    parameters:
      service-key:
        name: books1_html_repo_host-key
  - name: uaa_books1
    parameters:
      service-key:
        name: uaa_books1-key
  parameters:
    content:
      instance:
        destinations:
        - Name: booksCopyRouter_books1_html_repo_host
          ServiceInstanceName: books1-html5-app-host-service
          ServiceKeyName: books1_html_repo_host-key
          sap.cloud.service: booksCopyRouter
        - Authentication: OAuth2UserTokenExchange
          Name: booksCopyRouter_uaa_books1
          ServiceInstanceName: books1-xsuaa-service
          ServiceKeyName: uaa_books1-key
          sap.cloud.service: booksCopyRouter
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: books1-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: books1_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - books1.zip
      name: books1
      target-path: resources/
- name: books1
  type: html5
  path: app/books1
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: books1-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: books1-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: books1-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: books1_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: books1-html5-app-host-service
    service-plan: app-host
- name: uaa_books1
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: books1-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
